{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    <h1>Créer ton compte</h1>

    {{ form_start(registrationForm) }}
    {{ form_row(registrationForm.name, { 'label': 'Nom Siouplait' }) }}
    {{ form_row(registrationForm.email, { 'label': 'Email Siouplait' }) }}
    {{ form_row(registrationForm.bio, { 'label': 'Description Siouplait' }) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Mot de Passe Siouplait'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}
        <button type="submit" class="btn btn-lg btn-primary">CpartiMonKiki</button>
    {{ form_end(registrationForm) }}
{% endblock %}
